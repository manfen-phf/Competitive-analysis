"use client";
import Link from "next/link"; import { useState } from "react";
export default function ImportPage(){const [passcode,setPasscode]=useState("");const [file,setFile]=useState<File|null>(null);const [effectiveFrom,setEffectiveFrom]=useState("");const [status,setStatus]=useState("");async function submit(){if(!file)return;const form=new FormData();form.set("passcode",passcode);form.set("file",file);form.set("effectiveFrom",effectiveFrom);const r=await fetch("/api/admin/master-data",{method:"POST",body:form});const body=await r.json();setStatus(body.error||body.errors?.join("；")||`导入成功：${body.imported} 条商家归属记录`)}return <main><Link href="/">← 首页</Link><h1>主数据导入</h1><p className="muted">兼容标准列“商家ID、商家名称、城市、BD姓名”，也兼容现有数据源“外卖组织结构、商家ID、商家名称、合作BD”。若文件没有生效开始日，请在此处填写。</p><div className="filters"><label>管理员口令<input type="password" value={passcode} onChange={e=>setPasscode(e.target.value)}/></label><label>本版本生效开始日<input type="date" value={effectiveFrom} onChange={e=>setEffectiveFrom(e.target.value)}/></label><label>Excel 数据源<input type="file" accept=".xlsx,.xls" onChange={e=>setFile(e.target.files?.[0]||null)}/></label><button className="primary" onClick={submit} disabled={!file||!passcode}>校验并导入</button></div>{status&&<p className="status">{status}</p>}</main>}
